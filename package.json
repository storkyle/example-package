{
  "name": "example-package",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": " yarn compile",
    "compile": "tsc --project tsconfig.json && tsc-alias -p tsconfig.json",
    "test": "echo \"Error: no test specified\""
  },
  "author": "",
  "license": "ISC",
  "publishConfig": {
    "@storkyle:registry": "https://npm.pkg.github.com"
  },
  "devDependencies": {
    "tsc-alias": "^1.8.8",
    "typescript": "^5.3.3"
  }
}
